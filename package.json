{
  "name": "test",
  "version": "0.1.0",
  "bin": {
    "test": "bin/test.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "^2.44.0",
    "jest": "^27.5.1"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.44.0",
    "aws-sdk": "^2.1222.0",
    "constructs": "^10.0.0"
  }
}
